line-length = 100
target-version = "py311"

exclude = [".venv", "dist", "build"]

[lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[lint.per-file-ignores]
# Tests say what they mean; a long assertion message is worth more than a short line.
"**/tests/*" = ["E501"]
